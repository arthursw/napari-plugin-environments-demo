[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "napari-isolation-demo-worker"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools]
py-modules = ["napari_isolation_worker"]
